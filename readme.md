#serverless create --template aws-nodejs --path serverless-aws-lambda-nodejs

#serverless config credentials -o --provider aws --key=[key-aws] --secret [scret-aws]

#serverless deploy -v

#serverless invoke -f hello -l 

#serverless remove